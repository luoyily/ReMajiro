[package]
name = "ruri"
version = "0.1.0"
edition = "2021"
description = "ruri game entry point: window, asset loading, engine bootstrap"

[[bin]]
name = "ruri"
path = "src/main.rs"

[dependencies]
game = { path = "../owarusekai" }
engine = { path = "../engine" }

[build-dependencies]
winresource = { version = "0.1.31", default-features = false }
